# Python (tectonics pipeline)
__pycache__/
*.pyc
reports/tectonics/cache/

# Node (regional-report)
node_modules/

# Dataset helper outputs (scripts/reassemble.py) — derived, do not commit
/orogen_regions_full.csv
/orogen_regions_full.csv.gz
/orogen_regions_full_v2.csv
/orogen_regions_full_v2.csv.gz

# Export-v2 pipeline intermediates (tools/export-v2/) — derived, do not commit
tools/export-v2/intermediate/

# OS / editor cruft
.DS_Store

# v3 derived cache — rebuildable from orogen_mesh_triangles + the region parts
# via scripts/rebuild_triangle_centroids.py. ~72 MB; not worth committing.
data/orogen_regions_v3_browser/orogen_triangles_xyz_elev.f32.gz

# typological-envelope: regenerable province cache
tools/typological-envelope/.cache/
